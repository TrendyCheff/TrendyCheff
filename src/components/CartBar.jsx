import { useCart } from '../context/CartContext.jsx';
export default function CartBar() {
  // Cart bar removed — cart info shown in OrderSummary side panel during catering/checkout
  return null;
}
